name: Copilot Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  copilot-review:
    name: Request Copilot review
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write

    steps:
      - name: Request Copilot code review
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh api repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/requested_reviewers \-f reviewers='["copilot"]'